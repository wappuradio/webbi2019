# Wappuradio Front

Use nodejs 22 (or preferably update the dependencies) in order to get a succesful build

## `yarn install`+ `yarn start`
[http://localhost:3000](http://localhost:3000)

## `yarn build`
Builds the app for production to the `build` folder.
